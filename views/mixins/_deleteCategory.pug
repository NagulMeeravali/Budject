mixin deleteCategoryForm(category={})
  form(action=`/category/${category.slug}/delete` method="POST" class="grid" onsubmit="return confirm('Are you sure you want to delete this category?');")
    .form-group.grid-cell.w100
      input(type="submit" value=`Delete ${category.title} Category`)