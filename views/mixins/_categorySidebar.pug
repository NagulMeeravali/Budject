mixin categorySidebar(category = {})
  aside
    ul.category-list
      each category in categories
        li
          a(href=`/category/${category.slug}`) 
            span #{category.title} (#{h.formatPrice(category.amount)})
          a.icon(href=`/category/${category.slug}/edit` title="Edit") 
            != h.icon('edit')