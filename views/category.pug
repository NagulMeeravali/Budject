extends layout

include mixins/_categories
include mixins/_item

block sidebar
  aside
    +categories(categories)

block content

  != h.dump(itemSum)
  
  != h.dump(category)
  h1 #{title} - #{itemSum}/#{h.formatPrice(category.amount)}

  each entry in category.entries
    +item(entry)




